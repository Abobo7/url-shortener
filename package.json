{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "A modern URL shortener hosted on Vercel",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "nanoid": "^5.0.4"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
